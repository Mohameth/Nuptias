{% extends "IUTNuptiasBundle::layout.html.twig" %}

{% block title %}
  {{ parent() }} - Espace personnel - Liste de vos mariages
{% endblock %}

{% block body %}

  <h1> Espace Personnel </h1>
  <h3> Liste de vos mariages : </h3>
  {% set i = 1 %}

  {% for mariage in listeMariage %}
    <div class="container">
  	<div class="row">
  		<div class='list-group gallery'>
              <div class='col-sm-4 col-xs-6 col-md-3 col-lg-3'>
                  <p> Mariage n°{{ i }} </p>
                  <p> Date : {{ mariage.date|date('d/m/Y') }}</p>
                  <p> Ville : {{ mariage.ville }} </p>

                  <a class="btn btn-primary btn-info" href="{{ path('iut_nuptias_dashBoard', {'id': mariage.id }) }}">Modifier</a>
              </div>
      </div>
    </div>
    </div>
    {% set i = i + 1 %}
  {% endfor %}
{% endblock %}
